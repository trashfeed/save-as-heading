{
  "name": "save-as-heading",
  "main": "./dist/save-as-heading",
  "version": "0.5.1",
  "description": "This package can use the first line as the file name to quickly create or overwrite the file.",
  "keywords": [
    "save",
    "filename",
    "firstline",
    "headingline",
    "head",
    "markdown"
  ],
  "activationCommands": {
    "atom-workspace": "save-as-heading:save"
  },
  "repository": "https://github.com/trashfeed/save-as-heading",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "@types/atom": "^1.24.0"
  }
}
